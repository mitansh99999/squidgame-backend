{
  "version": 1,
  "builds": [
    {
      "src": "server.js",  // Entry point for your app
      "use": "@vercel/node"  // Use the Vercel Node.js builder
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
